{
  "name": "digitalhub-core",
  "version": "0.16.0-SNAPSHOT",
  "private": true,
  "scripts": {
    "prettier": "prettier --config ./.prettierrc.yml --write --list-different \"modules/*/src/**/*.java\"  \"application/src/**/*.java\"",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s -r 1 --skip-unstable",
    "i18n": "find ./schemas -name '*.json' | xargs -i cat {} | grep -e \"title\" -e \"description\" | grep \"\\.\" | tr -d \\\" | tr -d \\, | awk -F ':' '{print $2}' | awk '{$1=$1};1' | sort -u > I18N_KEYS.txt",
    "generate-schemas": "mvn spring-boot:run -pl application -Dspring-boot.run.profiles=generate-schemas && rm -rf schemas/specs && mv application/specs schemas/",
    "generate-envs": "mvn spring-boot:run -pl application -Dspring-boot.run.profiles=generate-envs && rm -f ENV_VARS.txt && mv application/ENV_VARS.txt ENV_VARS.txt",
    "generate-configuration-envs": "bash scripts/generate-configuration-envs.sh > CONFIGURATION.md"
  },
  "devDependencies": {
    "conventional-changelog-cli": "^5.0.0",
    "prettier": "^3.2.5",
    "prettier-plugin-java": "^2.8.1"
  },
  "dependencies": {},
  "eslintConfig": {}
}
